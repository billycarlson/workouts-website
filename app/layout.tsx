import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import Script from "next/script";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Workout Calendar",
  description: "A local-first calendar for functional strength workouts.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Script id="display-xxl-boot" strategy="beforeInteractive">
          {`try{if(localStorage.getItem("display-xxl")==="1"){document.documentElement.classList.add("display-xxl")}}catch(e){}`}
        </Script>
        {children}
      </body>
    </html>
  );
}
